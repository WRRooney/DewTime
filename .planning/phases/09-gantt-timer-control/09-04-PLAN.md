---
phase: 09-gantt-timer-control
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - src/renderer/src/components/TabStrip.tsx
  - src/renderer/src/components/TabStrip.module.css
  - src/renderer/src/components/TabStrip.test.tsx
  - src/renderer/src/components/DateNavToolbar.tsx
  - src/renderer/src/components/AppFooter.tsx
  - src/renderer/src/components/AppFooter.test.tsx
autonomous: true
requirements: [SC-1, SC-6, D-01, D-02, D-03, D-30, D-31]
user_setup: []

must_haves:
  truths:
    - "A TabStrip renders exactly three tabs labelled Timers | Gantt | Projects in that order (D-01), with the active tab visually distinguished"
    - "Clicking a tab updates useActiveTabStore (and persists via its write-through)"
    - "The DateNavToolbar title row shows the TabStrip in place of the old Timers h1 (D-02), with the Week-total pill still beside it (D-03)"
    - "The AppFooter no longer renders a Projects button (D-30) but still renders the version button that opens releases (D-31)"
  artifacts:
    - path: "src/renderer/src/components/TabStrip.tsx"
      provides: "Three-tab navigation bound to useActiveTabStore"
      contains: "Gantt"
    - path: "src/renderer/src/components/DateNavToolbar.tsx"
      provides: "Title row with <TabStrip/> replacing <h1>Timers</h1>, Week-total pill retained"
      contains: "TabStrip"
    - path: "src/renderer/src/components/AppFooter.tsx"
      provides: "Footer with version button only (Projects button removed, no onOpenProjects prop)"
      contains: "versionBtn"
  key_links:
    - from: "src/renderer/src/components/TabStrip.tsx"
      to: "src/renderer/src/stores/useActiveTabStore.ts"
      via: "reads tab + calls setTab"
      pattern: "useActiveTabStore"
    - from: "src/renderer/src/components/DateNavToolbar.tsx"
      to: "src/renderer/src/components/TabStrip.tsx"
      via: "renders <TabStrip/> in titleRow"
      pattern: "<TabStrip"
---

<objective>
Convert the app header into a tab strip and clean up the footer. Build the new `TabStrip` component (Timers | Gantt | Projects) bound to `useActiveTabStore`, swap it into `DateNavToolbar`'s title row in place of the `<h1>Timers</h1>` (keeping the Week-total pill, D-02/D-03), and remove the now-redundant footer Projects button while keeping the version button (D-30/D-31).

Purpose: SC-1 (tab strip, week-total pill on all tabs) and SC-6's footer cleanup. This is independent of the gantt canvas internals — it only needs the tab store from plan 09-03 — so it runs in parallel with the gantt-primitives plan. The actual tab-conditional rendering of the three views in `<main>` happens in the composition plan (09-06), which owns App.tsx.

Output: New TabStrip (+ CSS + test), modified DateNavToolbar, modified AppFooter (+ updated test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-gantt-timer-control/09-PATTERNS.md
@.planning/phases/09-gantt-timer-control/09-UI-SPEC.md
@.planning/phases/09-gantt-timer-control/09-03-SUMMARY.md

<interfaces>
From src/renderer/src/stores/useActiveTabStore.ts (created in 09-03):
- `useActiveTabStore((s) => s.tab)` → 'timers' | 'gantt' | 'projects'
- `useActiveTabStore((s) => s.setTab)` → (tab: ActiveTab) => void (persists via write-through)
- `type ActiveTab`

From src/renderer/src/components/DateNavToolbar.tsx (file being modified, title row ~lines 47-59):
- `<div className={styles.titleRow}><h1 className={styles.title}>Timers</h1><span className={styles.weekTotalLabel}>Week total</span><span className={styles.weekPill}><WeeklyTotal .../></span></div>`
- `WeeklyTotal` import + `weekRange.fromEpoch/toEpoch` already in scope — KEEP these

From src/renderer/src/components/AppFooter.tsx (file being modified, full):
- `interface AppFooterProps { onOpenProjects: () => void }` (REMOVE this prop)
- `export function AppFooter({ onOpenProjects }: AppFooterProps)` → becomes no-prop
- `useAppVersion()` + `handleOpenReleases()` + version button (`styles.versionBtn`) — KEEP
- Projects button block (the one using `onClick={onOpenProjects}`) — REMOVE

From src/renderer/src/components/AppFooter.test.tsx (file being modified):
- existing test passes `onOpenProjects` and asserts the Projects button — update: remove Projects-button assertions, assert version button still opens releases, assert no Projects button rendered

UI-SPEC values (09-UI-SPEC.md §"Tab Strip"): tab height 28px; active = `--font-weight-semibold` + 2px bottom border `--color-accent` + `--color-fg`; inactive = `--font-weight-normal` + `--color-fg-muted`; hover bg `--color-bg-hover`; focus ring `outline: 2px solid var(--color-accent)`. Labels EXACTLY "Timers"/"Gantt"/"Projects" (D-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TabStrip component + CSS + test</name>
  <files>src/renderer/src/components/TabStrip.tsx, src/renderer/src/components/TabStrip.module.css, src/renderer/src/components/TabStrip.test.tsx</files>
  <read_first>
    - src/renderer/src/components/AppFooter.tsx — button rendering + className composition pattern (`${styles.btn} ${styles.x}`)
    - src/renderer/src/components/DateNavToolbar.module.css — nav button height/color token usage (the 28px/border/token pattern to match)
    - src/renderer/src/stores/useActiveTabStore.ts — the store this binds to (from 09-03)
    - src/renderer/src/components/ConfirmDialog.test.tsx OR ProjectCell.test.tsx — `// @vitest-environment jsdom` + Testing Library render/click pattern
    - .planning/phases/09-gantt-timer-control/09-PATTERNS.md §"src/renderer/src/components/TabStrip.tsx" (exact TABS array, render, and CSS module)
    - .planning/phases/09-gantt-timer-control/09-UI-SPEC.md §"Tab Strip" + §"Color" + §"Typography"
  </read_first>
  <behavior>
    - Renders exactly three buttons with text "Timers", "Gantt", "Projects" in that order (D-01)
    - Each button has role="tab"; the active tab has aria-selected="true" and the active class; others aria-selected="false"
    - Clicking the "Gantt" button calls useActiveTabStore setTab('gantt')
  </behavior>
  <action>
    Create `TabStrip.tsx` per PATTERNS: a `TABS` array `[{id:'timers',label:'Timers'},{id:'gantt',label:'Gantt'},{id:'projects',label:'Projects'}]`, read `tab`/`setTab` via primitive selectors from `useActiveTabStore`, render a `<nav className={styles.tabStrip} aria-label="Main navigation">` of `<button type="button" role="tab" aria-selected={active} className={active ? tab+tabActive : tab} onClick={() => setTab(id)}>`. Create `TabStrip.module.css` per UI-SPEC: `.tabStrip` flex + `flex:1` (takes the old `.title` space) + `gap: var(--space-1)`; `.tab` height 28px, `padding 0 var(--space-2)`, transparent bg, `--color-fg-muted`, `border-bottom: 2px solid transparent`, `--font-size-sm`, `--font-weight-normal`, pointer cursor; `.tab:hover { background: var(--color-bg-hover) }`; `.tab:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px }`; `.tabActive { color: var(--color-fg); font-weight: var(--font-weight-semibold); border-bottom-color: var(--color-accent) }`. Zero hex literals — tokens only. Create `TabStrip.test.tsx` (`// @vitest-environment jsdom`) asserting the three labels/order, aria-selected on the active tab, and that clicking Gantt calls the store setter.
  </action>
  <verify>
    <automated>npx vitest run --config vitest.renderer.config.ts src/renderer/src/components/TabStrip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TabStrip renders three buttons with exact labels Timers/Gantt/Projects in order (test asserts) per D-01
    - `grep -c "#" src/renderer/src/components/TabStrip.module.css` returns 0 (no hex literals; tokens only)
    - active tab class applies `border-bottom-color: var(--color-accent)` and `font-weight: var(--font-weight-semibold)` (source assertion)
    - clicking the Gantt tab calls the store's setTab with 'gantt' (test asserts)
    - Named vitest command passes
  </acceptance_criteria>
  <done>TabStrip renders the three exact tabs bound to the store, styled with tokens per UI-SPEC, with a passing test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap TabStrip into DateNavToolbar title row; strip Projects button from AppFooter</name>
  <files>src/renderer/src/components/DateNavToolbar.tsx, src/renderer/src/components/AppFooter.tsx, src/renderer/src/components/AppFooter.test.tsx</files>
  <read_first>
    - src/renderer/src/components/DateNavToolbar.tsx — file being modified; title row block (~lines 47-59) with `<h1>Timers</h1>` + Week-total pill
    - src/renderer/src/components/AppFooter.tsx — file being modified; the AppFooterProps interface, the Projects button block, and the version button block
    - src/renderer/src/components/AppFooter.test.tsx — file being modified; existing assertions referencing onOpenProjects / Projects button
    - .planning/phases/09-gantt-timer-control/09-PATTERNS.md §"DateNavToolbar.tsx (modified)" and §"AppFooter.tsx (modified)" (exact before/after)
    - .planning/phases/09-gantt-timer-control/09-CONTEXT.md D-02, D-03, D-30, D-31
  </read_first>
  <behavior>
    - DateNavToolbar title row renders <TabStrip/> and still renders the Week-total label + WeeklyTotal pill beside it (D-03); the <h1>Timers</h1> is gone (D-02)
    - AppFooter renders the version button (opens releases) and renders NO Projects button; it accepts no onOpenProjects prop (D-30/D-31)
  </behavior>
  <action>
    In `DateNavToolbar.tsx`: import `TabStrip` and replace `<h1 className={styles.title}>Timers</h1>` with `<TabStrip />` inside the existing `titleRow` div; leave the `weekTotalLabel` span and `weekPill`/`WeeklyTotal` untouched (D-03). In `AppFooter.tsx`: remove the `AppFooterProps` interface and the `{ onOpenProjects }` destructure (component becomes `export function AppFooter(): JSX.Element`), delete the Projects `<button>` block (D-30), keep the `useAppVersion()` + `handleOpenReleases` + version `<button>` exactly as-is (D-31). Update `AppFooter.test.tsx`: drop the `onOpenProjects` prop and any Projects-button assertion; assert the version button renders and `onClick` triggers `window.api.system.openReleases` (or the existing releases handler the current test uses), and assert there is no button with the Projects label/aria.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run --config vitest.renderer.config.ts src/renderer/src/components/AppFooter.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<h1" src/renderer/src/components/DateNavToolbar.tsx` returns 0 and `grep -c "<TabStrip" src/renderer/src/components/DateNavToolbar.tsx` is at least 1 (D-02)
    - DateNavToolbar still references `WeeklyTotal` and `weekPill` (Week-total pill retained, source assertion per D-03)
    - `grep -c "onOpenProjects" src/renderer/src/components/AppFooter.tsx` returns 0 (prop + Projects button removed per D-30)
    - AppFooter still references `versionBtn` and `handleOpenReleases` (version button retained per D-31)
    - AppFooter.test.tsx passes with no Projects-button assertions and a version-button-opens-releases assertion; typecheck clean (note: App.tsx still passes `onOpenProjects` until plan 09-06 — the typecheck command here may surface that; if so it is resolved in 09-06 which owns App.tsx. Confirm AppFooter + DateNavToolbar themselves typecheck via their own usages.)
  </acceptance_criteria>
  <done>DateNavToolbar shows the tab strip with the week pill retained; AppFooter is Projects-button-free and prop-free while keeping the version button; tests updated and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational rewiring; the only state mutation is via useActiveTabStore.setTab which persists through the already-validated settings IPC (plan 09-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | TabStrip writing an invalid tab value to settings | mitigate | setTab is typed to `ActiveTab`; settings `SetArgsSchema` enum branch (plan 09-01) rejects any non-enum value at the IPC boundary |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — RESEARCH §"Package Legitimacy Audit" confirms zero new deps. |
</threat_model>

<verification>
- `npx vitest run --config vitest.renderer.config.ts src/renderer/src/components/TabStrip.test.tsx src/renderer/src/components/AppFooter.test.tsx` green
- grep gates: no `<h1` in DateNavToolbar (D-02), `<TabStrip` present, no `onOpenProjects` in AppFooter (D-30), no hex literals in TabStrip.module.css
- DateNavToolbar retains WeeklyTotal pill (D-03); AppFooter retains version button (D-31)
</verification>

<success_criteria>
- TabStrip (Timers|Gantt|Projects per D-01) bound to useActiveTabStore, styled per UI-SPEC
- DateNavToolbar title row uses TabStrip (D-02), Week-total pill retained (D-03)
- AppFooter Projects button removed (D-30), version button kept (D-31); tests updated
</success_criteria>

<output>
Create `.planning/phases/09-gantt-timer-control/09-04-SUMMARY.md` when done
</output>
