---
phase: 09-gantt-timer-control
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/utils/gantt-math.ts
  - src/renderer/src/utils/gantt-math.test.ts
autonomous: true
requirements: [SC-2, SC-3, D-06, D-07, D-08, D-18, D-27]
user_setup: []

must_haves:
  truths:
    - "epochToX and xToEpoch round-trip an epoch through pixel space within rounding tolerance for a given viewport (D-06 continuous transform)"
    - "snapEpoch rounds to the nearest grid increment, and returns the input unchanged when the Alt key is held (D-18 Alt free-drag)"
    - "snapIncrementFor returns a zoom-aware grid increment that scales with viewport span (D-27 zoom-aware grid)"
    - "Viewport span clamps are documented constants: min 3600s (1 hour), max 604800s (7 days) (D-08)"
    - "DEFAULT_SPAN_SECONDS = 86400 is exported so callers initialize to the current-day span (D-07 default zoom = current day)"
  artifacts:
    - path: "src/renderer/src/utils/gantt-math.ts"
      provides: "GanttViewport interface + epochToX/xToEpoch/snapEpoch/snapIncrementFor pure functions"
      exports: ["GanttViewport", "epochToX", "xToEpoch", "snapEpoch", "snapIncrementFor"]
    - path: "src/renderer/src/utils/gantt-math.test.ts"
      provides: "RED-first unit tests for all four functions + clamp constants"
      contains: "snapIncrementFor"
  key_links:
    - from: "src/renderer/src/utils/gantt-math.ts"
      to: "@shared/time"
      via: "imports EpochSeconds branded type only (no store/IPC imports)"
      pattern: "from '@shared/time'"
---

<objective>
Build `gantt-math.ts` — the pure epoch↔pixel transform and zoom-aware snap module that the gantt canvas, axis header, and drag interactions all consume. This is a dependency-free pure-function module (the cleanest possible TDD candidate) so it lands in Wave 1 alongside the backend, fully tested, before any component imports it.

Purpose: All gantt time math must stay in epoch seconds (no `Date.now()`, no date library) and must be deterministic and unit-tested. Isolating it as a pure module keeps the heavy interactive components (Wave 3) thin and lets snap/zoom thresholds be tuned without touching React code.

Output: `gantt-math.ts` with `GanttViewport`, `epochToX`, `xToEpoch`, `snapEpoch`, `snapIncrementFor`, plus a passing test suite covering round-trip, Alt-free-drag, snap brackets, and clamp constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-gantt-timer-control/09-RESEARCH.md
@.planning/phases/09-gantt-timer-control/09-PATTERNS.md

<interfaces>
From src/renderer/src/utils/date-ranges.ts (the analog — pure epoch module):
- module header comment states `Date.now()` is FORBIDDEN; functions are pure; return branded `EpochSeconds`
- shape: `export function dayRangeOf(date: Date): { fromEpoch: EpochSeconds; toEpoch: EpochSeconds }`

From src/renderer/src/utils/date-ranges.test.ts (the test analog):
- `// @vitest-environment jsdom` header is NOT required for pure-math utils; existing util tests run under the renderer config
- uses `describe`/`it`/`expect` from vitest

From @shared/time:
- `EpochSeconds` branded type (the only import this module needs)
</interfaces>
</context>

<feature>
  <name>gantt-math pure transform + snap module</name>
  <files>src/renderer/src/utils/gantt-math.ts, src/renderer/src/utils/gantt-math.test.ts</files>
  <read_first>
    - src/renderer/src/utils/date-ranges.ts — pure-module pattern: header comment forbidding Date.now(), branded EpochSeconds returns, no store/IPC imports
    - src/renderer/src/utils/date-ranges.test.ts — vitest pure-util test pattern to mirror
    - .planning/phases/09-gantt-timer-control/09-PATTERNS.md §"src/renderer/src/utils/gantt-math.ts (utility, transform)" — exact function signatures and bodies
    - .planning/phases/09-gantt-timer-control/09-RESEARCH.md §"Pattern 3" (epoch↔pixel) + §"Validation Architecture" Wave 0 Gaps (this test file is listed as a required Wave 0 scaffold)
  </read_first>
  <behavior>
    - epochToX(epoch, vp) = ((epoch - vp.startEpoch) / vp.spanSeconds) * vp.canvasWidthPx — left edge maps to x=0; (startEpoch + spanSeconds) maps to x=canvasWidthPx (D-06 continuous horizontal transform)
    - xToEpoch(x, vp) is the inverse: xToEpoch(epochToX(e, vp), vp) ≈ e (within 1s tolerance for a representative viewport)
    - snapEpoch(epoch, increment, altKey=false) returns Math.round(epoch/increment)*increment; snapEpoch(epoch, increment, true) returns epoch unchanged (D-18 Alt free-drag)
    - snapIncrementFor(span): span<=3600 → 60; <=10800 → 300; <=43200 → 900; <=86400 → 1800; else → 3600 (D-27 zoom-aware grid)
    - The min/max span clamp constants (3600 and 604800 per D-08) are exported or asserted so callers (GanttView wheel handler) reuse one source of truth
  </behavior>
  <implementation>
    Write the RED tests first in gantt-math.test.ts covering every <behavior> bullet (round-trip, Alt branch, each snapIncrementFor bracket boundary, and the clamp constants for D-07 default-day span 86400 / D-08 min 3600 / max 604800). Then implement gantt-math.ts exactly per PATTERNS §gantt-math: header comment forbidding raw `Date.now()`, import only `EpochSeconds` from `@shared/time`, export `interface GanttViewport { startEpoch: EpochSeconds; spanSeconds: number; canvasWidthPx: number }` and the four pure functions. Export the clamp bounds as named constants (e.g. `MIN_SPAN_SECONDS = 3600`, `MAX_SPAN_SECONDS = 604800`) and `DEFAULT_SPAN_SECONDS = 86400` so plan 09-06's wheel-zoom clamp imports them (D-07/D-08). No imports of stores, IPC, or React.
  </implementation>
  <verify>
    <automated>npx vitest run --config vitest.renderer.config.ts src/renderer/src/utils/gantt-math.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `gantt-math.ts` exports `GanttViewport`, `epochToX`, `xToEpoch`, `snapEpoch`, `snapIncrementFor`, and span constants `MIN_SPAN_SECONDS`/`MAX_SPAN_SECONDS`/`DEFAULT_SPAN_SECONDS`
    - `grep -vc '^//' src/renderer/src/utils/gantt-math.ts | : ` — module contains no `Date.now(` call (source assertion: `grep -c "Date.now(" src/renderer/src/utils/gantt-math.ts` returns 0) and no import from a store or `window.api`
    - Test asserts `xToEpoch(epochToX(e, vp), vp)` round-trips within 1s for a 86400s/1000px viewport
    - Test asserts `snapEpoch(e, 900, true) === e` (Alt) and `snapEpoch` rounds to nearest 900 when alt=false
    - Test asserts `snapIncrementFor(3600)===60`, `snapIncrementFor(86400)===1800`, `snapIncrementFor(604800)===3600`
    - Test asserts `MIN_SPAN_SECONDS===3600`, `MAX_SPAN_SECONDS===604800`, `DEFAULT_SPAN_SECONDS===86400`
    - Named vitest command passes
  </acceptance_criteria>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure renderer math module — no IPC, no persisted state, no external input crosses a trust boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | gantt-math producing out-of-range epochs that later reach createEntry/setTimestamps | accept | This module only computes candidate values; the IPC boundary (plan 09-01 Zod schemas) is the authoritative validation. No DB write happens here. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — RESEARCH §"Package Legitimacy Audit" confirms zero new dependencies this phase. |
</threat_model>

<verification>
- `npx vitest run --config vitest.renderer.config.ts src/renderer/src/utils/gantt-math.test.ts` green
- `grep -c "Date.now(" src/renderer/src/utils/gantt-math.ts` returns 0 (epoch-source rule honored)
- Module imports only `EpochSeconds` from `@shared/time` (no store/IPC/React imports)
</verification>

<success_criteria>
- gantt-math.ts is a pure, dependency-free module with the four documented functions and clamp constants
- All behavior bullets covered by passing RED→GREEN tests
- D-06 (continuous horizontal transform), D-07 (DEFAULT_SPAN_SECONDS = 86400), D-08 (MIN/MAX_SPAN_SECONDS clamps), D-18 (Alt free-drag via snapEpoch altKey branch), D-27 (zoom-aware snap brackets via snapIncrementFor) all encoded and asserted
</success_criteria>

<output>
Create `.planning/phases/09-gantt-timer-control/09-02-SUMMARY.md` when done
</output>
